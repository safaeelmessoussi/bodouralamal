# Local Egress config (R99). Dev overlay only — never used in production.
log_level: debug
api_key: devkey
api_secret: devsecretdevsecretdevsecretdevsecret
ws_url: ws://livekit:7880
redis:
  address: redis:6379
insecure: true
